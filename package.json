{
  "name": "coinscan",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ coinscan",
    "create-local": "graph create --node http://localhost:8020/ coinscan",
    "remove-local": "graph remove --node http://localhost:8020/ coinscan",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 coinscan"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
